Used for building / cleaning strings

s.replace(old, new)
s.strip()        # remove leading & trailing whitespace
s.lstrip()
s.rstrip()
s.split(delim)   # string → list
delim.join(list) # list → string

🔥 Most tested:

replace()

split()

join()

strip()

⚠️ Interview tip:

join() is the correct way to build strings efficiently.
