Used for substring problems & validation

s.find(sub)        # index or -1
s.index(sub)       # index or error
s.count(sub)       # frequency
s.startswith(sub)
s.endswith(sub)

🔥 Most tested:

find()

count()

startswith()

endswith()

⚠️ find() > index() for safety in interviews.
